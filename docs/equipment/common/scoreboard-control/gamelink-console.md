---
title: Colorado Time Systems GameLink console
description: >-
  The GameLink console is a Colorado Time Systems keyboard console that entered team-sport
  data for a matrix display.
tags:
  - Equipment
  - Scoring
infoboxTitle: GameLink console
infobox:
  - label: Manufacturer
    value: Colorado Time Systems
  - label: Type
    value: Keyboard console for team-sport scoring data
  - label: Status
    value: Discontinued
  - label: Manual
    value: Template Editor 1.5 Users Guide (F855 Rev. 0404)
---

<!--
Research notes, GameLink console.

The GameLink name covers two things in CTS material, and the manuals do not always make
clear which is meant. One is the team-sports data module inside DisplayLink 3.x, reached
from that program's Game menu, which is covered in the DisplayLink article. The other is
this: an external console and keyboard that could feed a matrix display directly.

Attestation for the hardware:
sources/vendors/colorado-time-systems/software/cts-displaylink-template-editor-1.5-for-matrix-and-video-display-f855.pdf
(F855 Rev. 0404) lists the devices and programs that can send data into DisplayLink
templates and names an external GameLink console among them, alongside an external PC and
ASCII text sources. The same guide's standalone-template section says a standalone template
downloaded to the display tells it where to place data arriving from the external GameLink
keyboard or from a CTS sports timer, with no DisplayLink computer in the path, and that CTS
considered this a fallback rather than normal operation. The extraction of that page has
lost a bulleted list of programs; read the PDF itself before writing the body, because the
list of what can feed a template is exactly the kind of thing this page should get right.

The DisplayLink Plus article records an open question about whether GameLink shipped as its
own SKU. Nothing held answers it.

Still to research before building this out: part numbers, what the console physically was
(a keyboard alone, or a keyboard plus controller), which sports it covered, what it
connected to, and when CTS withdrew it. A CTS catalogue from the 1990s or 2000s is the
likely source; the ones held do not cover it.
-->

This article is a stub. The GameLink console is a Colorado Time Systems keyboard console
that entered team-sport scoring data, either as an input to
[DisplayLink](../../../software/displaylink.md) or, with a standalone template loaded, to a
matrix display with no display computer in the path.

See the [scoreboard control overview](index.md) for the shared background, and the
[scoreboards overview](../scoreboard/index.md) for the displays it fed.
